# Freight 24 Messaging — Baileys Listener Environment
# Copy this file to .env and fill in your values
# Never commit .env to git

# Your Supabase project URL
SUPABASE_URL=https://mwgygfjufeynkewntdgq.supabase.co

# Supabase service role key (required for server-side scripts — bypasses RLS)
# Get it from: Supabase → Settings → API → service_role key
# NEVER commit this key to git or share it publicly
SUPABASE_ANON_KEY=your_service_role_key_here
